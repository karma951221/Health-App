name: app_shared
description: Shared utilities (uuid, debounce, env, logger, result, date) for daylog.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  uuid: ^4.5.1
  intl: ^0.20.2
  envied: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  envied_generator: ^1.1.1
  build_runner: ^2.4.13
  fake_async: ^1.3.3
