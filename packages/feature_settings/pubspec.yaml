name: feature_settings
description: Settings and configuration management for the health app (pure Dart)
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  shared_preferences: ^2.2.0
  freezed_annotation: ^3.0.0
  json_annotation: ^4.12.0
  app_shared:
    path: ../app_shared

dev_dependencies:
  build_runner: ^2.4.0
  freezed: ^3.0.0
  json_serializable: ^6.8.0
