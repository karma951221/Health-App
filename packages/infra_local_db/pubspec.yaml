name: infra_local_db
description: Local DB repository interfaces and domain entities for daylog.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  app_shared:
    path: ../app_shared
  
  # Drift dependencies
  drift: ^2.14.1
  sqlite3_flutter_libs: ^0.5.17
  path_provider: ^2.1.1
  path: ^1.8.3

  freezed_annotation: ^3.0.0
  json_annotation: ^4.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.6
  drift_dev: ^2.14.1
  freezed: ^3.2.5
  json_serializable: ^6.8.0
